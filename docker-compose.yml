version: "3.8"

services:

  calculator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rf-production-prediction
    volumes:
      - .:/workspace
      - mlruns:/workspace/mlruns
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      - mlflow
    restart: "no"
    networks:
      - ml-network

  mlflow:
    image: python:3.11-slim
    container_name: mlflow-server
    command: >
      bash -c "pip install mlflow==2.13.2 --quiet &&
               mlflow server
               --host 0.0.0.0
               --port 5000
               --backend-store-uri /mlruns
               --default-artifact-root /mlruns"
    ports:
      - "5000:5000"
    volumes:
      - mlruns:/mlruns
    restart: unless-stopped
    networks:
      - ml-network

volumes:
  mlruns:

networks:
  ml-network:
    driver: bridge